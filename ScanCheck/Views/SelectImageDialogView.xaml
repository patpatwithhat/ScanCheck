<metro:MetroWindow  xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls" x:Class="ScanCheck.Views.SelectImageDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScanCheck.Views"
        mc:Ignorable="d"
        Title="Selected Image" TitleCharacterCasing="Normal" Height="450" Width="800">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="{Binding InfoText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button x:Name="Save" Content="Save Image" Margin="5" Padding="5,0"/>
            <Button x:Name="Cancel" Content="Cancel" Margin="5" Padding="5,0" />
            
        </StackPanel>
        <Border DockPanel.Dock="Top" BorderBrush="DarkGray" BorderThickness="2" CornerRadius="2" Margin="5">
            <TextBlock Text="This is your selected image. Do you want to save it?" Margin="5" />
        </Border>
        <Image x:Name="Image" Source="{Binding Image.Path}" Stretch="Uniform" />
    </DockPanel>
</metro:MetroWindow>
