<metro:MetroWindow  xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls" x:Class="ScanCheck.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScanCheck.Views"
        xmlns:conv="clr-namespace:ScanCheck.Converters"
        mc:Ignorable="d"
        Title="ScanCheck" TitleCharacterCasing="Normal" Height="450" Width="800">
    <metro:MetroWindow.Resources>
        <conv:EmptyStringToBooleanConverter x:Key="EmptyStringToBooleanConverter" Invert="False" />

    </metro:MetroWindow.Resources>
    <DockPanel>

        <Grid  DockPanel.Dock="Top" Margin="5" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center">Select Folder:</TextBlock>
            <TextBox Text="{Binding SelectedFolderPath}"  Grid.Column="1"></TextBox>
            <Button x:Name="BrowseFolder" Grid.Column="2" Margin="5,0,0,0">...</Button>
        </Grid>
        <Grid  Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" IsEnabled="{Binding SelectedFolderPath, Converter={StaticResource EmptyStringToBooleanConverter}, ConverterParameter=True}">Left</Button>
            <Button Grid.Column="1">Right</Button>
        </Grid>
    </DockPanel>
</metro:MetroWindow>
